env: "development"  # или "production", "staging"
CONFIG_PATH: "configs/config.yaml"
server:
  port: 8082
  host: "localhost"
  timeout: 4s
  idle_timeout: 30s


database:
  type: "postgres"
  host: "localhost"
  port: 5432
  name: "postgres"
  user: "postgres"
  password: "252566"

logging:
  level: info
  format: json  # или "text"
  output: stdout
